# FLT7-RAMIFIED-009 verification and inference report

## Outcome

Outcome A.

The discriminant-49 integral cubic carrier, both determinant-norm source
formulas, the ramified prime identity, and the unit-free pure-axis
normalization are fixed in Lean. The RAMIFIED-008 receiver packet is connected
to this carrier without adding a new branch assumption.

## Lean facts fixed

The new module `SevenRealCubicInt.lean` constructs the free integral
three-coordinate order generated by `alpha` and proves:

```text
alpha^3 = 2*alpha^2 + alpha - 1

Norm(x*y) = Norm(x)*Norm(y)

Norm(a - alpha*n) = leftCubic(a,n)
Norm(a + (1 + alpha)*n) = rightCubic(a,n).
```

The displayed left and right monic cubics both have discriminant `49`.

For

```text
pi      = 1 + 2*alpha
epsilon = -1 + 2*alpha + 4*alpha^2
```

Lean verifies:

```text
Norm(pi) = -7
epsilon = alpha*(1+alpha)^2
pi^3 = 7*epsilon
Norm(epsilon) = -1
epsilon * (-9 + 22*alpha - 8*alpha^2) = 1.
```

Thus `epsilon` is an actual unit in the implemented order, not merely a
norm-unit candidate.

With

```text
varpi = epsilon^4*pi
Z(m)  = epsilonInv^8*varpi*m,
```

the exact normalization theorem is:

```text
pi * (7^4*m^7) = varpi^6 * Z(m)^7.
```

No division or fractional carrier is used.

## Connection to RAMIFIED-008

`SevenBaseTerminalRamifiedRealCubicNorm.lean` packages the receiver data as:

```text
innerRoot.snd = 7^4*m^7
leftCubic(innerRoot) = l^7
rightCubic(innerRoot) = r^7.
```

It then proves:

```text
Norm(etaL) = l^7
Norm(etaR) = r^7

r^7 - l^7 = 7*a*n*(a+n)

etaR - etaL = pi*n
etaR - etaL = varpi^6*Z(m)^7.
```

This is the precise meeting point of the integer signed-root projection and
the real-cubic norm projection.

## Verification

Focused local builds passed for:

```text
DkMath.FLT.Seven.SevenRealCubicInt
DkMath.FLT.Seven.SevenBaseTerminalRamifiedRealCubicNorm
```

The exported theorem axiom audits contain no `sorryAx`. Only the standard
`propext`, `Classical.choice`, and `Quot.sound` dependencies reported by
Mathlib tactics remain.

## Inference and corrected boundary

The computation confirms the predicted pure second-case source shape, but
it does not yet yield:

```text
etaL = xiL^7
etaR = xiR^7.
```

A seventh-power norm is insufficient for element-level seventh-power
extraction. The missing path is now sharply separated:

1. identify the explicit order as the full ring of integers;
2. prove class number one, likely using the discriminant-49 Minkowski bound;
3. determine the unit group modulo seventh powers;
4. prove the required global-to-local unit-class injection;
5. principalize and remove the unit classes of both source elements.

The small integer-side continuation is also isolated as RAMIFIED-009B:
transfer exact depth four from `n` to `|r-l|`, normalize the seventh-power
difference quotient, and build the resulting `2 x 3` routing. That work is
independent of cubic-order maximality and should not be confused with the
completed RAMIFIED-009 carrier checkpoint.

## Stop statement

RAMIFIED-009 stops here as designed. No maximal-order, class-number, unit
generator, local-injection, source seventh-power, smaller Fermat solution, or
recursive descent theorem is claimed.
