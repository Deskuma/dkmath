# FLT7-FUSION-004B U1.4 element-level oriented power report

Date: 2026-07-30

Execution mode: ULTRA

Active phase: U1

Event: U1.4

Starting commit: `675a5746`

Ending commit: this U1.4 Event commit

## Completed Lean facts

The ring of integers of every seventh cyclotomic number field is now proved
to be principal:

```text
CyclotomicSeven.ringOfIntegers_isPrincipalIdealRing
CyclotomicSeven.classNumber_eq_one.
```

The proof computes a Minkowski floor at most four.  Rational primes below the
bound are therefore only two and three.  Their residue degrees in the
seventh cyclotomic field are respectively

```text
orderOf (2 : ZMod 7) = 3
orderOf (3 : ZMod 7) = 6,
```

so the norms of prime ideals above them are already greater than the bound.

For the concrete degree-six carrier, Lean proves

```text
zetaInv = zeta^6
Algebra.adjoin ℤ {zeta} = top.
```

The explicit six integer coordinates are generated by `zeta`: first
`zeta + zetaInv + 1` recovers the real cubic generator, then every real
coordinate and every quadratic coordinate is generated.

Mathlib's integral power basis of the abstract seventh cyclotomic ring of
integers has minimal polynomial `cyclotomic 7 ℤ`.  Evaluating that power basis
at the concrete `zeta` defines

```text
SevenCyclotomicDegreeSixInt.ringOfIntegersToRing :
  𝓞 (CyclotomicField 7 ℚ) →ₐ[ℤ]
    SevenCyclotomicDegreeSixInt.Ring.
```

Because the concrete carrier is generated by `zeta`, this map is surjective:

```text
ringOfIntegersToRing_surjective.
```

Principality descends through the surjection, yielding the concrete instance

```text
SevenCyclotomicDegreeSixInt.ringIsPrincipalIdealRing.
```

No injectivity, rank comparison, integral-closure proof, or full
ring-of-integers identification is used.

The generic extraction theorem

```text
exists_mul_pow_of_span_eq_mul_pow
```

takes an ideal identity

```text
span {a} = L * R^n
```

and constructs generators `l,r` satisfying

```text
span {l} = L
span {r} = R
a = l*r^n.
```

Equality of principal ideals initially supplies an associated unit.  Lean
multiplies the generator of `L` by the inverse unit.  This absorbs the unit
without changing `span {l}` and does not assert that the unit is an
`n`-th power.

Specializing to the U1.3 ideals constructs

```text
orientedLoadElement
orientedResidualRoot
conjugateLoadElement
conjugateResidualRoot
```

with exact equations

```text
cyclotomicDegreeSixCarrier
  = orientedLoadElement * orientedResidualRoot^7

cyclotomicDegreeSixCarrierConj
  = conjugateLoadElement * conjugateResidualRoot^7.
```

Their ideal provenance is exact:

```text
span {orientedLoadElement}
  = globalOrientedLoadedCarrierIdeal
span {orientedResidualRoot}
  = globalOrientedResidualIdeal,
```

and similarly on the conjugate side.  The conjugate elements are defined
literally as the quadratic stars of the oriented elements.  These facts are
bundled in

```text
elementLevelOrientedConjugatePowerPacket.
```

## New modules

```text
SevenRamifiedFusionCyclotomicSevenPID.lean
SevenRamifiedFusionCyclotomicDegreeSixPID.lean
SevenRamifiedFusionElementLevelOrientedPower.lean
```

The final element-level module is imported by the public
`DkMath.FLT.Seven` facade.

## Mathematical interpretation

U1.4 has Outcome A in the corrected loaded form forced by U1.3.  The ideal
seventh power is now represented by a genuine seventh power of one concrete
carrier element.  The non-seventh-power ramified and routed-load factors are
represented by one concrete loaded element with exact ideal support.
Quadratic conjugation makes the two carrier equations coherent.

The associated-unit issue does not obstruct this corrected form: it is
legitimately absorbed into the loaded generator.  This is not a proof that
the unit, the loaded element, or either scalar routing cell is a seventh
power.

## Exact remaining obligation

The two equations above are multiplicative identities in the cyclotomic
carrier.  Event U1.5 requires an actual additive seventh-power identity on
integer or otherwise canonically normalized coordinates.  Coefficient
projection and the product over six Galois phases reproduce coordinate and
norm ledgers, but they do not automatically create a Fermat triple.

The next event must therefore either:

- extract three nonzero primitive integer coordinates from the residual root
  and prove their additive seventh-power identity; or
- formalize the exact compatibility receiver missing from the current
  element packet.

The old direct signed-root candidate cannot be reused: Lean has already proved
that its seventh-power difference has exact seven-adic depth five.

This Event does not claim:

- that `orientedLoadElement` is a seventh power;
- that either scalar routed load is a seventh power;
- that an arbitrary carrier unit is a seventh power;
- a primitive additive Fermat chart;
- strict decrease, descent closure, or FLT7.

## Build verification

All three focused module builds and the public facade build succeeded.  The
new modules contain no `sorry`, explicit `axiom`, or `native_decide`.
Printed axiom dependencies are limited to the standard `propext`,
`Classical.choice`, and `Quot.sound`.

## Outcome

Outcome A: U1.4 is complete in the exact loaded form, with star-compatible
element-level seventh-power witnesses.

Next selected Event:

```text
ULTRA / U1.5 — primitive additive chart candidate
```
