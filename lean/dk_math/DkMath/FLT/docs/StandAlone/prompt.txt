あのね。ワークスペース内にある補題をかき集めて１ファイル化したいのね。エラーのある補題を探して単純コピペ。

namespace も無くしていまいたいので補題名だけにする。
名前の衝突は無いと思うのだけど、あった場合は、namespace で隔離する。逆に open 指定で省略されているものは、修飾する Finset とか Nat など。

持ってくる補題定理は *.lean ファイル限定。ドキュメントや *.txt から持ってくるのはダメ。

ペースト位置は、エラーの出ていた補題の真上。

この単一 Lean ファイルがビルドが通るまで繰り返す。
エラーの修正はしてはダメ。必ず、オリジナルコードのコピーペーストだけで解決する。
但し、namespace の修飾は調整修正して良い。

sorry を含む補題は対象外。

参照していない補題も持ってこない。
docstringは不要。定義と実装部。

エラーは lint 機能を使う。もしくは、
ビルドチェックを行うなら

$ cd /home/deskuma/develop/lean/dkmath/lean/dk_math && ./lean-build.sh DkMath.FLT.docs.StandAlone.a

これで、ログが確認できる。

という簡単な作業、出来る？やってみて。